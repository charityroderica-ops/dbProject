DB_HOST=localhost
DB_USER=root
DB_PASS=
DB_NAME=db_webdev
PORT=3000

- POST `/api/register` (username, password, email)
- POST `/api/login` (username, password)
- GET `/api/profile/:username`
- POST `/api/products` (name, price, description, category, image_url)
- GET `/api/products` (View all catalog)